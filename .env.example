# Discord Bot Token（Discordデベロッパーポータルで取得）
DISCORD_TOKEN=your_discord_bot_token_here

# テキストコマンドのプレフィックス（デフォルト: /）
# COMMAND_PREFIX=/

# PostgreSQL接続設定（いずれか一方を設定してください）
#
# 方法①: 接続URLで指定
# 書式: postgresql://<ユーザー名>:<パスワード>@<ホスト>:<ポート>/<データベース名>
DATABASE_URL=postgresql://your_db_user:your_db_password@localhost:5432/your_db_name
#
# 方法②: 個別パラメータで指定（DATABASE_URLが未設定の場合に使用されます）
# ポートフォワーディング等でポート番号が異なる場合もここで指定できます
# DB_HOST=192.168.122.60
# DB_PORT=35272          # ポートフォワーディング先のポート番号（例: 35272 → 内部5432）
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_NAME=postgres

# PostgreSQL SSL接続を有効にするか（本番/クラウドDBでは true、ローカル/同一ホストでは false）
DB_SSL=false


# 以下の4つをすべて設定するとダッシュボードが有効になります

# ダッシュボードのポート番号（デフォルト: 3000）
DASHBOARD_PORT=3000

# 開発時に Next.js dev サーバー (port 3001) からExpressのAPIサーバー (port 3000) へ接続するためのURL
# 開発時: http://localhost:3000  本番時: 不要（同一サーバーのため空文字でOK）
# NEXT_PUBLIC_API_URL=http://localhost:3000

# セッション署名用のランダム文字列（必須）
SESSION_SECRET=your_random_session_secret_here

# Discord OAuth2 アプリのクライアントID（Discordデベロッパーポータルで取得）
DISCORD_CLIENT_ID=your_discord_client_id_here

# Discord OAuth2 アプリのクライアントシークレット
DISCORD_CLIENT_SECRET=your_discord_client_secret_here

# OAuth2 コールバックURL（Discordデベロッパーポータルの「Redirects」に登録したものと一致させる）
DISCORD_REDIRECT_URI=http://localhost:3000/auth/callback
